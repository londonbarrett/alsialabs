## 1. Search Input and Filtering

- [x] 1.1 Add `searchQuery` state and `useMemo` for filtered clients in `ClientListView`
- [x] 1.2 Add search `Input` with `SearchIcon` to the header area between title and buttons
- [x] 1.3 Implement case-insensitive filtering across name, phone, email, location, and comments fields
- [x] 1.4 Replace table with empty state message when search yields no results
